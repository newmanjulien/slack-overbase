# Shared code

This folder contains cross-app contracts and utilities shared by:
- The main Slack app (this repo)
- The responder Slack app (separate repo)
- The portal app (if needed)

Current contracts:
- `shared/relay/types.ts` — relay envelope + file metadata shared across apps.
